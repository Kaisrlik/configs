# example of ccls file, please edit and copy it to .ccls
g++
%c -std=c11
%cpp -std=c++2a
-Isrc/include
# CMAKE compile commadns are generated via - cmake -DCMAKE_EXPORT_COMPILE_COMMANDS=1
# meson build # generates compile_commands.json in the `build` directory
